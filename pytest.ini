[pytest]
# run from repo root: `pytest` (all) or `pytest -m "not slow"` (fast only)
pythonpath = .
testpaths = tests
markers =
    slow: tests that load the real embedding model / build a real KB (skip with -m "not slow")
